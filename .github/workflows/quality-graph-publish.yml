# Generated by Quality Graph. DO NOT EDIT MANUALLY. Update qg.yaml and run qg generate.
---
name: Quality Graph Publisher
'on':
  workflow_run:
    workflows:
      - Quality Graph
    types:
      - requested
      - completed
  issue_comment:
    types:
      - created
      - edited
permissions: {}
concurrency:
  group: quality-graph-publish-${{ github.event.workflow_run.id || github.event.issue.number }}
  cancel-in-progress: false
jobs:
  publish:
    name: Publish Quality Graph
    if: github.event_name == 'workflow_run' && github.event.workflow_run.event == 'pull_request'
    runs-on: ubuntu-latest
    permissions:
      actions: read
      checks: write
      contents: read
      issues: write
      pull-requests: write
    steps:
      - name: Publish trusted Quality Graph state
        uses: alchemmist/quality-graph@585b63cf6fd2c022160a10a1330be4799e9d5acc
        with:
          operation: ${{ github.event.action == 'completed' && 'publish' || 'watch' }}
  command:
    name: Handle Quality Graph command
    if: github.event_name == 'issue_comment' && github.event.issue.pull_request
    runs-on: ubuntu-latest
    permissions:
      actions: write
      contents: read
      issues: write
      pull-requests: write
    steps:
      - name: Handle trusted Quality Graph command
        uses: alchemmist/quality-graph@585b63cf6fd2c022160a10a1330be4799e9d5acc
        with:
          operation: command
