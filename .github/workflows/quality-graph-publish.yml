# Generated by Quality Graph. Do not edit.
---
name: Quality Graph Publisher
'on':
  workflow_run:
    workflows:
    - Quality Graph
    types:
    - requested
    - in_progress
    - completed
  issue_comment:
    types:
    - created
    - edited
permissions: {}
concurrency:
  group: quality-graph-publish-${{ github.event.workflow_run.id || github.event.issue.number }}
  cancel-in-progress: false
jobs:
  publish:
    name: Publish Quality Graph
    if: github.event_name == 'workflow_run' && github.event.workflow_run.event == 'pull_request'
    runs-on: ubuntu-latest
    permissions:
      actions: read
      checks: write
      contents: read
      issues: write
      pull-requests: read
    steps:
    - name: Publish trusted Quality Graph state
      uses: alchemmist/quality-graph@4707114b53f3add10c3fe0e4dfc673944768c498
      with:
        operation: publish
  command:
    name: Handle Quality Graph command
    if: github.event_name == 'issue_comment' && github.event.issue.pull_request
    runs-on: ubuntu-latest
    permissions:
      actions: write
      contents: read
      issues: write
      pull-requests: read
    steps:
    - name: Handle trusted Quality Graph command
      uses: alchemmist/quality-graph@4707114b53f3add10c3fe0e4dfc673944768c498
      with:
        operation: command
