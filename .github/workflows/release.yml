# Generated by Quality Graph. DO NOT EDIT MANUALLY. Update qg.yaml and run qg generate.
---
name: Release
'on':
  push:
    tags:
      - v[0-9]+.[0-9]+.[0-9]+
permissions:
  contents: read
concurrency:
  group: quality-graph-release-publishing
  cancel-in-progress: false
jobs:
  build:
    name: Release Build (build)
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          persist-credentials: 'false'
      - uses: astral-sh/setup-uv@37802adc94f370d6bfd71619e3f0bf239e1f3b78
      - run: uv sync --locked --all-groups
      - name: Verify release version
        run: test "$GITHUB_REF_NAME" = "v$(uv run --locked --all-packages qg --version)"
      - run: make check BASE="$GITHUB_SHA"
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: quality-graph-core
          path: dist/quality_graph_core-*
          if-no-files-found: error
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: quality-graph-python
          path: dist/quality_graph_python-*
          if-no-files-found: error
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: quality-graph-github
          path: dist/quality_graph_github-*
          if-no-files-found: error
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: quality-graph-cli
          path: dist/quality_graph_cli-*
          if-no-files-found: error
      - name: Collect Release Build (build) result
        id: quality-result
        if: always()
        uses: alchemmist/quality-graph@4e24fc581b2e7b9357b62f5e76d60543f3266277
        with:
          operation: collect
          node-id: build
          title: Release Build (build)
          adapter: exit-code
          report-path: ''
          command-outcome: ${{ job.status }}
          graph-digest: 34f0b35e30145ee7ca74338e6873cb1b212f12e3c479e857686f079048fb46b0
          approval-findings: 'false'
          approval-files: 'false'
          approval-node: 'false'
          flow-id: release
          operation-id: release-build
          presentation: release
      - name: Upload Release Build (build) result
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: quality-result-build-${{ github.run_attempt }}
          path: ${{ runner.temp }}/quality-graph/build.json
          if-no-files-found: error
          retention-days: 7
      - name: Enforce Release Build (build) result
        if: always()
        shell: bash
        env:
          EXIT_CODE: ${{ steps.quality-result.outputs.exit-code }}
        run: exit "${EXIT_CODE:-2}"
    if: startsWith(github.ref, 'refs/tags/')
    env:
      QG_FLOW_ID: release
      QG_OPERATION_ID: release-build
      QG_NODE_ID: build
      QG_NODE_TITLE: Release Build (build)
      QG_GRAPH_DIGEST: 34f0b35e30145ee7ca74338e6873cb1b212f12e3c479e857686f079048fb46b0
  publish-core:
    name: Publish Core
    runs-on: ubuntu-latest
    permissions:
      id-token: write
    steps:
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
        with:
          name: quality-graph-core
          path: dist
      - uses: pypa/gh-action-pypi-publish@dc37677b2e1c63e2034f94d8a5b11f265b73ba33
      - name: Collect Publish Core result
        id: quality-result
        if: always()
        uses: alchemmist/quality-graph@4e24fc581b2e7b9357b62f5e76d60543f3266277
        with:
          operation: collect
          node-id: publish-core
          title: Publish Core
          adapter: exit-code
          report-path: ''
          command-outcome: ${{ job.status }}
          graph-digest: 34f0b35e30145ee7ca74338e6873cb1b212f12e3c479e857686f079048fb46b0
          approval-findings: 'false'
          approval-files: 'false'
          approval-node: 'false'
          flow-id: release
          operation-id: publish-core
          presentation: release
      - name: Upload Publish Core result
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: quality-result-publish-core-${{ github.run_attempt }}
          path: ${{ runner.temp }}/quality-graph/publish-core.json
          if-no-files-found: error
          retention-days: 7
      - name: Enforce Publish Core result
        if: always()
        shell: bash
        env:
          EXIT_CODE: ${{ steps.quality-result.outputs.exit-code }}
        run: exit "${EXIT_CODE:-2}"
    needs:
      - build
    environment:
      name: pypi
      url: https://pypi.org/project/quality-graph-core/
    if: startsWith(github.ref, 'refs/tags/')
    env:
      QG_FLOW_ID: release
      QG_OPERATION_ID: publish-core
      QG_NODE_ID: publish-core
      QG_NODE_TITLE: Publish Core
      QG_GRAPH_DIGEST: 34f0b35e30145ee7ca74338e6873cb1b212f12e3c479e857686f079048fb46b0
  publish-python:
    name: Publish Python
    runs-on: ubuntu-latest
    permissions:
      id-token: write
    steps:
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
        with:
          name: quality-graph-python
          path: dist
      - uses: pypa/gh-action-pypi-publish@dc37677b2e1c63e2034f94d8a5b11f265b73ba33
      - name: Collect Publish Python result
        id: quality-result
        if: always()
        uses: alchemmist/quality-graph@4e24fc581b2e7b9357b62f5e76d60543f3266277
        with:
          operation: collect
          node-id: publish-python
          title: Publish Python
          adapter: exit-code
          report-path: ''
          command-outcome: ${{ job.status }}
          graph-digest: 34f0b35e30145ee7ca74338e6873cb1b212f12e3c479e857686f079048fb46b0
          approval-findings: 'false'
          approval-files: 'false'
          approval-node: 'false'
          flow-id: release
          operation-id: publish-python
          presentation: release
      - name: Upload Publish Python result
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: quality-result-publish-python-${{ github.run_attempt }}
          path: ${{ runner.temp }}/quality-graph/publish-python.json
          if-no-files-found: error
          retention-days: 7
      - name: Enforce Publish Python result
        if: always()
        shell: bash
        env:
          EXIT_CODE: ${{ steps.quality-result.outputs.exit-code }}
        run: exit "${EXIT_CODE:-2}"
    needs:
      - build
    environment:
      name: pypi-python
      url: https://pypi.org/project/quality-graph-python/
    if: startsWith(github.ref, 'refs/tags/')
    env:
      QG_FLOW_ID: release
      QG_OPERATION_ID: publish-python
      QG_NODE_ID: publish-python
      QG_NODE_TITLE: Publish Python
      QG_GRAPH_DIGEST: 34f0b35e30145ee7ca74338e6873cb1b212f12e3c479e857686f079048fb46b0
  publish-github:
    name: Publish Github
    runs-on: ubuntu-latest
    permissions:
      id-token: write
    steps:
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
        with:
          name: quality-graph-github
          path: dist
      - uses: pypa/gh-action-pypi-publish@dc37677b2e1c63e2034f94d8a5b11f265b73ba33
      - name: Collect Publish Github result
        id: quality-result
        if: always()
        uses: alchemmist/quality-graph@4e24fc581b2e7b9357b62f5e76d60543f3266277
        with:
          operation: collect
          node-id: publish-github
          title: Publish Github
          adapter: exit-code
          report-path: ''
          command-outcome: ${{ job.status }}
          graph-digest: 34f0b35e30145ee7ca74338e6873cb1b212f12e3c479e857686f079048fb46b0
          approval-findings: 'false'
          approval-files: 'false'
          approval-node: 'false'
          flow-id: release
          operation-id: publish-github
          presentation: release
      - name: Upload Publish Github result
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: quality-result-publish-github-${{ github.run_attempt }}
          path: ${{ runner.temp }}/quality-graph/publish-github.json
          if-no-files-found: error
          retention-days: 7
      - name: Enforce Publish Github result
        if: always()
        shell: bash
        env:
          EXIT_CODE: ${{ steps.quality-result.outputs.exit-code }}
        run: exit "${EXIT_CODE:-2}"
    needs:
      - build
      - publish-core
    environment:
      name: pypi-github
      url: https://pypi.org/project/quality-graph-github/
    if: startsWith(github.ref, 'refs/tags/')
    env:
      QG_FLOW_ID: release
      QG_OPERATION_ID: publish-github
      QG_NODE_ID: publish-github
      QG_NODE_TITLE: Publish Github
      QG_GRAPH_DIGEST: 34f0b35e30145ee7ca74338e6873cb1b212f12e3c479e857686f079048fb46b0
  publish-cli:
    name: Publish Cli
    runs-on: ubuntu-latest
    permissions:
      id-token: write
    steps:
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
        with:
          name: quality-graph-cli
          path: dist
      - uses: pypa/gh-action-pypi-publish@dc37677b2e1c63e2034f94d8a5b11f265b73ba33
      - name: Collect Publish Cli result
        id: quality-result
        if: always()
        uses: alchemmist/quality-graph@4e24fc581b2e7b9357b62f5e76d60543f3266277
        with:
          operation: collect
          node-id: publish-cli
          title: Publish Cli
          adapter: exit-code
          report-path: ''
          command-outcome: ${{ job.status }}
          graph-digest: 34f0b35e30145ee7ca74338e6873cb1b212f12e3c479e857686f079048fb46b0
          approval-findings: 'false'
          approval-files: 'false'
          approval-node: 'false'
          flow-id: release
          operation-id: publish-cli
          presentation: release
      - name: Upload Publish Cli result
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: quality-result-publish-cli-${{ github.run_attempt }}
          path: ${{ runner.temp }}/quality-graph/publish-cli.json
          if-no-files-found: error
          retention-days: 7
      - name: Enforce Publish Cli result
        if: always()
        shell: bash
        env:
          EXIT_CODE: ${{ steps.quality-result.outputs.exit-code }}
        run: exit "${EXIT_CODE:-2}"
    needs:
      - build
      - publish-core
    environment:
      name: pypi-cli
      url: https://pypi.org/project/quality-graph-cli/
    if: startsWith(github.ref, 'refs/tags/')
    env:
      QG_FLOW_ID: release
      QG_OPERATION_ID: publish-cli
      QG_NODE_ID: publish-cli
      QG_NODE_TITLE: Publish Cli
      QG_GRAPH_DIGEST: 34f0b35e30145ee7ca74338e6873cb1b212f12e3c479e857686f079048fb46b0
  release:
    name: Github Release (release)
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
        with:
          pattern: quality-graph-*
          path: dist
          merge-multiple: 'true'
      - name: Create immutable GitHub release
        run: gh release create "$GITHUB_REF_NAME" dist/* --generate-notes --verify-tag
          --repo "$GITHUB_REPOSITORY"
        env:
          GH_TOKEN: ${{ github.token }}
      - name: Collect Github Release (release) result
        id: quality-result
        if: always()
        uses: alchemmist/quality-graph@4e24fc581b2e7b9357b62f5e76d60543f3266277
        with:
          operation: collect
          node-id: release
          title: Github Release (release)
          adapter: exit-code
          report-path: ''
          command-outcome: ${{ job.status }}
          graph-digest: 34f0b35e30145ee7ca74338e6873cb1b212f12e3c479e857686f079048fb46b0
          approval-findings: 'false'
          approval-files: 'false'
          approval-node: 'false'
          flow-id: release
          operation-id: github-release
          presentation: release
      - name: Upload Github Release (release) result
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: quality-result-release-${{ github.run_attempt }}
          path: ${{ runner.temp }}/quality-graph/release.json
          if-no-files-found: error
          retention-days: 7
      - name: Enforce Github Release (release) result
        if: always()
        shell: bash
        env:
          EXIT_CODE: ${{ steps.quality-result.outputs.exit-code }}
        run: exit "${EXIT_CODE:-2}"
    needs:
      - publish-core
      - publish-python
      - publish-github
      - publish-cli
    if: startsWith(github.ref, 'refs/tags/')
    env:
      QG_FLOW_ID: release
      QG_OPERATION_ID: github-release
      QG_NODE_ID: release
      QG_NODE_TITLE: Github Release (release)
      QG_GRAPH_DIGEST: 34f0b35e30145ee7ca74338e6873cb1b212f12e3c479e857686f079048fb46b0
