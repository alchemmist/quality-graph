# Quality Graph GitHub Provider

GitHub workflow generation, trusted publication, and Action runtime for Quality Graph.

The distribution registers `github` in the `qg.providers` entry-point group and depends only on
`quality-graph-core`. It never imports the CLI.
