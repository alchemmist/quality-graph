# Generated by Quality Graph. DO NOT EDIT MANUALLY.
---
workflow:
  rules:
  - if: $CI_COMMIT_REF_PROTECTED == "true" && $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
      && ($CI_PIPELINE_SOURCE == "trigger" || $CI_PIPELINE_SOURCE == "schedule" ||
      $CI_PIPELINE_SOURCE == "web")
  - when: never
publish:
  image: python:3.12-slim
  tags:
  - qg-publisher
  variables:
    GIT_STRATEGY: none
  resource_group: quality-graph-publication
  before_script:
  - python -m pip install --disable-pip-version-check quality-graph-gitlab==0.2.0
  script:
  - qg-gitlab publish
  timeout: 10 minutes
