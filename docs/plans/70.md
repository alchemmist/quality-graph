# GitLab provider delivery plan

Issue: https://github.com/alchemmist/quality-graph/issues/70

## Workspace and baseline

- Branch `70`, isolated worktree, based on freshly fetched `origin/main`.
- Initial base: `01bfaab9e31e4ddd06b3c07c14f0d6332b68e384`.
- `make install`: passed.
- `make t-fast`: 475 passed, 73 deselected.
- Do not base this work on branch `69`. Carry the artifact-admission invariants
  into GitLab tests independently; integrate the GitHub fix when it reaches main.

## Delivery checkpoints

- [x] Isolated worktree and baseline checks.
- [ ] Real local GitLab, execution runner, protected publisher runner, seed projects.
- [ ] Prove event delivery, cancellation, retries, fork MR and merge gating.
- [ ] Independently installable `quality-graph-gitlab` provider/runtime.
- [ ] Provider-aware onboarding and backwards-compatible core contracts.
- [ ] GitLab-specific versioned provenance and fail-closed artifact admission.
- [ ] Deterministic GitLab compiler and single-execution command runtime.
- [ ] Stateful HTTP fake, Docker adapter and integration scenarios.
- [ ] Trusted publisher, managed summary, owned labels and stale-writer handling.
- [ ] Authorized reversible finding/file/node approvals through `/qg` comments.
- [ ] Full end-to-end matrix against the real local GitLab.
- [ ] Fifth package in build, version updates, PyPI publication and GitHub Release.
- [ ] Documentation, migration guide and clean distribution installation.
- [ ] Complete quality gate, review and ready PR.
- [ ] Post-release installation from PyPI, verified on the same local GitLab.

## Fixed product decisions

The consumer uses ordinary GitLab jobs without GitHub credentials. A separate
service project executes the trusted publisher without checking out consumer code.
Use job, merge-request and note events, not pipeline webhooks. Reconciliation
repairs missed events; a bounded watcher must be validated for resource use.

Results bind instance, execution project, target project/MR, SHA, pipeline, job,
graph, flow, operation and node. A retry has its own job identity. Old, conflicting,
missing or malformed evidence cannot establish current success. Apply approvals
only after admission and only to quality failures.

Keep GitHub Result v0 compatibility. GitLab must not import the GitHub package.
Support both node declarations and MR/branch operation flows. Reject unsupported
platform-specific settings explicitly. Do not overwrite unmanaged CI files.

The HTTP fake and real GitLab are separate test layers. Fake scenarios run in
process and over a container HTTP endpoint; real GitLab runs actual jobs and
webhooks. GitLab.com is not required for implementation or acceptance.

## Real GitLab test matrix

Clean install/init/generate/validate; MR/default/feature branch pipelines; no
duplicate full checks; success/failure/quality/cancel/skip; missing/malformed/stale/
conflicting artifacts; partial and full retries; new MR heads; authorized and
unauthorized commands; revoke and redelivery; note/label ownership; forks; delayed
publisher and recovery; credential isolation; installation from built wheels.

## Release contract

Release all five packages at one exact version. Add isolated `publish-gitlab`
with `pypi-gitlab` Trusted Publishing and include its wheel/sdist in the final
GitHub Release. Edit `qg.yaml`, regenerate workflows through Makefile targets,
and preserve historical migration fixtures. PR readiness does not imply a tag or
PyPI publication has occurred.

## Environment findings

The host is ARM64 with 24 GiB RAM and about 104 GiB available disk space.
The existing Podman machine has 2 GiB RAM. Podman refuses to run a second machine
concurrently, so the created but stopped `qg-gitlab` Podman machine is not used.
Use a dedicated Colima Docker profile instead; preserve the existing Podman VM.
Official GitLab CE 19.3.1 and Runner 19.3.0 images both provide ARM64 manifests.

## Progress log

- 2026-09-12: Created worktree, installed workspace, passed initial fast suite.
- 2026-09-12: Verified official ARM64 images; preparing isolated Docker runtime.
- 2026-09-12: Dedicated Colima Docker VM running; official GitLab CE 19.3.1 and
  Runner 19.3.0 pinned by multi-architecture image digest. Existing Podman VM is unchanged.
- 2026-09-12: Seeded consumer/publisher projects in `qg-lab/team`; execution and
  protected publisher runners online. Job events trigger the protected publisher
  directly through native GitLab webhooks.
- 2026-09-12: Built provider/core/CLI wheels and installed them in actual jobs
  from a local wheelhouse without a package index. MR 1, pipeline 12 passed after
  retry, with jobs 25/26 and a publisher-owned summary. Publisher uses its own
  Maintainer account token rather than the bootstrap administrator token.
- 2026-09-12: Fast suite: 516 passed. HTTP integration: 87 passed in-process and
  87 passed through Docker, including the first GitLab artifact-admission matrix.
  Type checking and candidate wheel/content checks passed. Remaining work includes
  retry admission barriers, full approval/fork/cancellation E2E coverage, release
  integration, documentation and complete coverage/static-analysis gates.
